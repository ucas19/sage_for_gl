        Let $\mu:=\lambda + (0,0,0,0,0) $. 
        \begin{equation}\notag
          \begin{aligned}
            P_\mu = #1
          \end{aligned}
        \end{equation}
        \begin{equation}\notag
          \begin{aligned}
            Pr_{\lambda}(P_\mu \otimes V_{#2}) &= 
            #2
                                               &=
            #3
          \end{aligned}
        \end{equation}
      Similarly, the set of indeterminate composition factors is $U = \{

\}$. We have $Pr_{\lambda}(P_\mu \otimes V_{#4}) = P_\lambda  $ by \cref{tab:verma-analysis_#5}. 
\begin{table}[htbp]
\centering
\caption{Calculation of Verma modules in $\operatorname{Filt}(P_*) \setminus \operatorname{Filt}(\prl(P_\lambda \otimes V_{#4})) $ } \label{tab:verma-analysis_#5}
\begin{tabular}{cc}
\toprule
\textbf{Proj. Cover} & \textbf{$\operatorname{Filt}(P_*) \setminus \operatorname{Filt}(\prl(P_\mu \otimes V_{#4})) $}  \\
     & \textbf{(via Prop.~1)}  \\
\midrule
$ #6 $ & \makecell{
#7
}\\ 
$ #8 $ & \makecell{
#9
}\\ 
\bottomrule
\end{tabular}
\vspace{0.5em}
\parbox{\linewidth}{\small
\textbf{Interpretation:} 
This table displays only selected computational results. This partial presentation does not affect our conclusion.
}
\end{table}


        Let $\mu:=\lambda + (0,0,0,0,0) $.
        \begin{equation}\notag
          \begin{aligned}
            P_\mu = P_{\lambda + \epsilon_1}
          \end{aligned}
        \end{equation}
        \begin{equation}\notag
          \begin{aligned}
            Pr_{\lambda}(P_\mu \otimes V_{\omega_1}) &=
            P_{\nu_1} + P_{\nu_2}
                                               &=
            Q_{\xi}
          \end{aligned}
        \end{equation}
      Similarly, the set of indeterminate composition factors is $U = \{
\}$. We have $Pr_{\lambda}(P_\mu \otimes V_{\omega_1}) = P_\lambda  $ by \cref{tab:verma-analysis_caseA}.
\begin{table}[htbp]
\centering
\caption{Calculation of Verma modules in $\operatorname{Filt}(P_*) \setminus \operatorname{Filt}(\prl(P_\lambda \otimes V_{\omega_1})) $ } \label{tab:verma-analysis_caseA}
\begin{tabular}{cc}
\toprule
\textbf{Proj. Cover} & \textbf{$\operatorname{Filt}(P_*) \setminus \operatorname{Filt}(\prl(P_\mu \otimes V_{\omega_1})) $}  \\
     & \textbf{(via Prop.～1)}  \\
\midrule
$ P_{\alpha} $ & \makecell{
M(\beta) \\ M(\gamma)
}\\
$ P_{\delta} $ & \makecell{
M(\epsilon)
}\\
\bottomrule
\end{tabular}
\vspace{0.5em}
\parbox{\linewidth}{\small
\textbf{Interpretation:}
This table displays only selected computational results. This partial presentation does not affect our conclusion.
}
\end{table}
